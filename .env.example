# TEMPLATE ONLY — copy the VARIABLE NAMES, never put real values in a committed file.
# Local:  set these in your shell before running the scripts (see README).
# Cloud:  set these as environment secrets in the Claude Code web UI.
# The real .env is git-ignored and must NEVER be pushed.

ALPACA_API_KEY_ID=
ALPACA_API_SECRET_KEY=
ALPACA_PAPER=1
